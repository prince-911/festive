<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Login</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body {
      background: linear-gradient(135deg, #878baa, #be94a5, #878baa, rgb(163, 175, 140));
      min-height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
    }

    .login-card {
      width: 100%;
      max-width: 850px;
      height: 500px;
      border-radius: 15px;
      overflow: hidden;
      box-shadow: 0 8px 24px rgba(0, 0, 0, 0.2);
      background-color: #fff;
      display: flex;
      flex-direction: row;
    }

    .login-img {
      width: 100%;
      height: 100%;
      object-fit: cover;
    }

    .form-container {
      padding: 40px;
      flex: 1;
    }

    .flash-message {
      background-color: #d4edda;
      color: #155724;
      border: 1px solid #c3e6cb;
      border-radius: 5px;
      padding: 10px;
      margin-bottom: 15px;
      text-align: center;
      animation: fadeOut 1s ease-out 2s forwards;
    }

    @keyframes fadeOut {
      to {
        opacity: 0;
        visibility: hidden;
      }
    }

    @media (max-width: 768px) {
      .login-card {
        flex-direction: column;
        height: auto;
      }

      .login-img {
        height: 250px;
      }
    }
  </style>
</head>
<body>
  <div class="login-card">
    <!-- Left: Image -->
    <div class="col-6 d-none d-md-block p-0">
      <img src="{{ url_for('static', filename='img/login.webp') }}" alt="Login" class="login-img">
    </div>

    <!-- Right: Form -->
    <div class="form-container col-12 col-md-6">
      {% with messages = get_flashed_messages(with_categories=true) %}
        {% if messages %}
          {% for category, message in messages %}
            <div class="flash-message">{{ message }}</div>
          {% endfor %}
        {% endif %}
      {% endwith %}

      <h3 class="text-center mb-4">Login</h3>
      <form action="/login" method="POST" autocomplete="off">
        {{ res.csrf_token }}
        {{ res.hidden_tag() }}

        <div class="mb-3">
          {{ res.userrole(class_="form-select") }}
        </div>

        <div class="mb-3">
          {{ res.email.label(class_="form-label") }}
          {{ res.email(class_="form-control", placeholder="User Name") }}
          {% for error in res.email.errors %}
            <div class="text-primary small">{{ error }}</div>
          {% endfor %}
        </div>

        <div class="mb-3">
          {{ res.password.label(class_="form-label") }}
          {{ res.password(class_="form-control", placeholder="Password") }}
          {% for error in res.password.errors %}
            <div class="text-primary small">{{ error }}</div>
          {% endfor %}
        </div>

        <div class="d-grid mb-3">
          {{ res.submit(class_="btn btn-primary") }}
        </div>

        <div class="text-center">
          <small>Don't have an account? <a href="/register">Register here</a></small>
        </div>
      </form>
    </div>
  </div>

  <!-- Bootstrap JS (optional) -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
