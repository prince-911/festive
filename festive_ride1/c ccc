<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Login</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    .login-container {
      min-height: 100vh;
      background: linear-gradient(135deg, #878baa, #be94a5, #878baa, rgb(163, 175, 140));
    }
    .login-card {
      max-width: 900px;
      border-radius: 15px;
      overflow: hidden;
      box-shadow: 0 4px 20px rgba(0, 0, 0, 0.2);
    }
    .login-img {
      object-fit: cover;
      height: 100%;
      width: 100%;
    }
    .flash-message {
      background-color: #d4edda;
      color: #155724;
      border: 1px solid #c3e6cb;
      border-radius: 5px;
      padding: 10px;
      margin-bottom: 15px;
      text-align: center;
      animation: fadeOut 1s ease-out 2s forwards;
    }
    @keyframes fadeOut {
      to {
        opacity: 0;
        visibility: hidden;
      }
    }
  </style>
</head>
<body class="login-container d-flex justify-content-center align-items-center">
  <div class="card login-card w-100 row g-0">
    <div class="col-md-6">
      <img src="{{ url_for('static', filename='img/login.webp') }}" alt="Login Image" class="login-img">
    </div>
    <div class="col-md-6 bg-white p-4">
      {% with messages = get_flashed_messages(with_categories=true) %}
        {% if messages %}
          {% for category, message in messages %}
            <div class="flash-message">{{ message }}</div>
          {% endfor %}
        {% endif %}
      {% endwith %}

      <h3 class="text-center mb-4">Login</h3>
      <form action="/login" method="POST" autocomplete="off">
        {{ res.csrf_token }}
        {{ res.hidden_tag() }}

        <div class="mb-3">
          {{ res.userrole(class_="form-select") }}
        </div>

        <div class="mb-3">
          {{ res.email.label(class_="form-label") }}
          {{ res.email(class_="form-control", placeholder="User Name") }}
          {% for error in res.email.errors %}
            <div class="text-primary small">{{ error }}</div>
          {% endfor %}
        </div>

        <div class="mb-3">
          {{ res.password.label(class_="form-label") }}
          {{ res.password(class_="form-control", placeholder="Password") }}
          {% for error in res.password.errors %}
            <div class="text-primary small">{{ error }}</div>
          {% endfor %}
        </div>

        <div class="d-grid mb-3">
          {{ res.submit(class_="btn btn-primary") }}
        </div>

        <div class="text-center">
          <small>Don't have an account? <a href="/register">Register here</a></small>
        </div>
      </form>
    </div>
  </div>
</body>
</html>
